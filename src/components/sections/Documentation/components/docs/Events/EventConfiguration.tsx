
const EventConfiguration = () => {
    return (
        <article>
            <p>Navigate to <code>@src/config/events</code></p>
            <p>Here is where you can define your event subscribers and listeners, as well as define additional event drivers.</p>
        </article>

    );
}

export default EventConfiguration;
