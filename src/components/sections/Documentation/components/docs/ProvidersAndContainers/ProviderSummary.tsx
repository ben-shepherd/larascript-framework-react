
const ProviderSummary = () => {
    return (
        <section>
            <p>Providers are what's used to launch and configure your services while it is in boot up.</p>
            <p>A config file path can be provided which is automatically parsed and bound to the Provider.</p>
        </section>
    );
}

export default ProviderSummary