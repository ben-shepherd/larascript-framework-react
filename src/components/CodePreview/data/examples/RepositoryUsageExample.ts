
const RepositoryUsageExample = 
`const email = 'tony.stark@marvel.com';

const repository = new UserRepository();

const user = await repository.findOneByEmail(email);`

export default RepositoryUsageExample